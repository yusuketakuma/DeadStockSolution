# Deep Review Findings

## 重点テーマ: D. admin logs + openclaw/requests 連携 (2026-03-06 22:14 JST)

### 確認ファイル
1. server/src/routes/admin-log-center.ts
2. server/src/services/log-center-service.ts
3. server/src/services/openclaw-command-service.ts
4. server/src/routes/admin-logs.ts
5. server/src/services/openclaw-auto-handoff-service.ts

### セキュリティ（✅ 良好）
- ✅ 認証/認可: 全ルートで requireLogin + requireAdmin 適用
- ✅ SQLインジェクション対策: drizzle-orm によるパラメータ化クエリ
- ✅ 検索文字列エスケープ: escapeLikeWildcards() 使用
- ✅ コマンドホワイトリスト: isCommandAllowed() で検証
- ✅ パラメータ検証: Zod スキーマによる厳格なバリデーション
- ✅ 日時範囲検証: from ≤ to, 最大90日制限

### 正確性（✅ 良好）
- ✅ 日付範囲バリデーション: ISO 8601形式チェック、from ≤ to 検証
- ✅ ページネーション: offset/limit 正常に実装
- ✅ ログレベル正規化: 不正値は 'error' にフォールバック
- ✅ 重複排除: hasRecentAutoHandoff() で重複リクエスト防止

### 可読性（✅ 良好）
- ✅ 責務分離: route → service → db の明確な階層
- ✅ 命名規則: 一貫した英語命名、日本語コメント
- ✅ 型定義: LogSource, LogLevel, CommandDefinition 等の明示的型

### 速度（✅ 良好）
- ✅ getLogSummary(): 効率的な単一クエリ + FILTER句
- ✅ queryLogs(): マージソート方式（深いページでも許容範囲）
- ✅ 並列クエリ: Promise.all で複数テーブル同時取得

### 潜在課題（なし）
- levelConditionCache: キー数は最大12（3ソース × 4レベル）でbounded
- maintenance mode: process.env経由だが即時toggle用途として許容範囲
- collectOperationLogs失敗時: warningログ出力済み、handoff継続は設計通り

---

## 重点テーマ: C. upload confirm/parser/validation (2026-03-06 10:11 JST)

### 確認ファイル
1. server/src/services/upload-confirm-service.ts
2. server/src/services/drug-master-parser-service.ts
3. server/src/routes/upload-validation.ts

---

## 潜在課題 #1: CSV解析におけるDoS脆弱性 ✅ 解決済み (2026-03-06 22:08 JST)

### 実装内容
- ✅ CSV行長制限: MAX_CSV_LINE_LENGTH = 10000 (実装済み)
- ✅ ファイルサイズ制限: MAX_CSV_FILE_SIZE = 50MB (実装済み)
- ✅ 行数制限: MAX_CSV_ROWS = 100000 (実装済み)
- ✅ parseMhlwCsvData + parsePackageCsvData に適用
- ✅ テスト追加: 8テスト全て成功

### コミット
- 46a4dc8 feat(security): add CSV file size and row limits for DoS prevention

---

## ~~潜在的課題 #1: CSV解析におけるDoS脆弱性（未対策の行長制限）~~

### 再現条件
- 攻撃者が悪意あるCSVファイルをアップロード（またはMHLWデータソースが侵害される）
- CSVファイルに極端に長い行を含む（例：100万文字以上の1行）
- `parseMhlwCsvData()` → `parseCsvLine()` が呼び出される

### 影響
- **重大度**: High
- メモリ枯渇：1つの長い行が大量のメモリを消費
- CPU枯渇：文字列処理が長時間ブロック
- サーバー応答停止（DoS）
- 他のリクエストへの影響（リソース競合）

### 根本原因
`drug-master-parser-service.ts` lines 256-282:
```typescript
function parseCsvLine(line: string): string[] {
  const result: string[] = [];
  let current = '';
  let inQuotes = false;

  for (let i = 0; i < line.length; i++) {
    // line.length に制限なし
    // current 変数が際限なく成長
    const ch = line[i];
    // ...
  }
}
```

### 最小修正案
```typescript
const MAX_CSV_LINE_LENGTH = 10000; // 10KB制限

function parseCsvLine(line: string): string[] {
  if (line.length > MAX_CSV_LINE_LENGTH) {
    throw new Error(`CSV行が長すぎます（最大${MAX_CSV_LINE_LENGTH}文字）`);
  }
  
  const result: string[] = [];
  let current = '';
  let inQuotes = false;

  for (let i = 0; i < line.length; i++) {
    const ch = line[i];
    if (inQuotes) {
      if (ch === '"' && line[i + 1] === '"') {
        current += '"';
        i++;
      } else if (ch === '"') {
        inQuotes = false;
      } else {
        current += ch;
      }
    } else {
      if (ch === '"') {
        inQuotes = true;
      } else if (ch === ',') {
        result.push(current);
        current = '';
      } else {
        current += ch;
      }
    }
  }
  result.push(current);
  return result;
}
```

### 追加の防御策
```typescript
const MAX_CSV_FILE_SIZE = 50 * 1024 * 1024; // 50MB
const MAX_CSV_ROWS = 100000; // 10万行制限

export function parseMhlwCsvData(csvContent: string): ParsedDrugRow[] {
  if (csvContent.length > MAX_CSV_FILE_SIZE) {
    throw new Error('CSVファイルが大きすぎます');
  }
  
  const lines = csvContent.split(/\r?\n/);
  if (lines.length > MAX_CSV_ROWS) {
    throw new Error('CSV行数が上限を超えています');
  }
  
  // ... existing logic
}
```

---

## その他の確認事項

### セキュリティ（✅ 良好）
- ✅ SQLインジェクション対策: `computeHeaderHash()` はMD5ハッシュ化（16進数文字列）で安全
- ✅ Prototype Pollution対策: `__proto__`, `constructor`, `prototype` を除外（upload-validation.ts:112-117）
- ✅ パストラバーサル対策: ZIPエントリの`..`チェック実装済み（drug-master-parser-service.ts:427-431）
- ✅ ZIP爆弾対策: 200MB/entry, 500MB/total 制限実装済み（drug-master-parser-service.ts:422-437）

### 正確性（✅ 良好）
- ✅ 行ロック: `pg_advisory_xact_lock` で並行性制御（upload-confirm-service.ts:123）
- ✅ 型安全性: `Number.isInteger()`, `Number.isSafeInteger()` で検証

### 可読性（✅ 良好）
- ✅ 責務分離: パーサー、バリデーション、サービス層が明確に分離
- ✅ 命名: 日本語コメントと英語コードが適切に混在
- ✅ 定数管理: マジックナンバーは定数化（`INSERT_BATCH_SIZE`, `MAX_UPLOAD_SIZE`等）

### 速度（⚠️ 改善余地あり）
- ⚠️ `detectHeaderRow()` が最大15行スキャン（O(rows × headers × keywords)）
  - 影響: 軽微（初期行のみ、ヘッダー行は通常0-5行目）
  - 改善優先度: Low
- ⚠️ `insertInBatches()` が500件ずつ挿入
  - 影響: 大規模アップロード（10万件+）でトランザクション長期化
  - 改善優先度: Medium（現在は許容範囲）

---

## 次アクション
1. ~~**最優先**: CSV行長制限の実装（セキュリティ修正）~~ ✅ 完了
2. ~~テスト追加: 長いCSV行のエッジケース~~ ✅ 完了
3. ドキュメント: CSVファイル制限の明記（Low優先度）
4. 速度改善: detectHeaderRow() / insertInBatches()（Low-Medium優先度）

---

## 実行日時
- 開始: 2026-03-06 10:11:14 JST
- 終了: 2026-03-06 10:XX:XX JST（予定）
