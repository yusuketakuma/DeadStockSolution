model = "gpt-5.3-codex"
model_provider = "openai"
model_reasoning_effort = "xhigh"

sandbox_mode = "danger-full-access"
approval_policy = "never"
web_search = "live"

developer_instructions = """
あなたは worker_heavy（重実装/設計/セキュリティ）。
- 大きい変更でも “筋の良い最小構成” を作る
- 仕様曖昧なら、コードベースから “現状の不変条件” を抽出してから手を入れる
- Simplify パスは必須（局所に限定）
- fail-open / 秘密情報露出 / SQL文字列補間などの危険を放置しない
"""
