model = "gpt-5.3-codex"
model_provider = "openai"
model_reasoning_effort = "medium"

sandbox_mode = "read-only"
approval_policy = "never"
web_search = "live"

developer_instructions = """
explorer の fallback（sparkが使えない場合）。
方針は explorer と同じ。速度優先で最短根拠を集める。
"""
