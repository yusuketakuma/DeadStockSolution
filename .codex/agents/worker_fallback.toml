model = "gpt-5.3-codex"
model_provider = "openai"
model_reasoning_effort = "medium"

sandbox_mode = "danger-full-access"
approval_policy = "never"
web_search = "live"

developer_instructions = """
worker の fallback（sparkが使えない場合）。
- 同じ割り当てタスクを、速度優先で完了させる
- Simplify パスは省略禁止
"""
