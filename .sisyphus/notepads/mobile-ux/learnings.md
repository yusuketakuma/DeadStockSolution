- 2026-03-08: `ProposalDetailPage` 内のローカル描画ヘルパーを JSX コンポーネント (`<Helper />`) として使うと、再レンダーごとに要素タイプが変わってコメント編集欄が再マウントされる。ページ内ヘルパーは関数呼び出し (`Helper()`) に寄せると入力中の状態が安定する。
- 2026-03-08: Task 5 admin pages (AdminUploadJobsPage, AdminLogsPage, AdminExchangesPage) の mobile card-view 検証テストを追加。`AppResponsiveSwitch` + `AppMobileDataCard` の組み合わせで `.dl-mobile-data-list` コンテナが正しく描画されることを確認。テスト内で `setMatchMedia(true)` で mobile viewport を模擬し、desktop/mobile 両方の表示切り替えを検証。
- 2026-03-08: Dashboard timeline/digest テスト修正。`DashboardTimeline` は日付グループ化を実装していないため date-header テストを削除し、flat list 検証に変更。load-more button は testid なしなので `getByRole('button', { name: 'もっと見る' })` で取得。`SmartDigest` の critical badge は '重要' ではなく '緊急' を表示。`DashboardPage` の SmartDigest ヘッダーは '今日のアクション' ではなく '今日やること'。
- 2026-03-08: Camera multi-code test fix。`CameraDeadStockRegisterPanel` は fullscreen mode (`cameraFullscreen=true`) で UI が切り替わる。fullscreen 時は "撮影" ボタン、normal view 時は "画像からコード検出" ボタン。テストは fullscreen で撮影後、scan result sheet の "一覧" ボタンで normal view に戻して rows を確認。lazy-loaded camera section は `waitFor` で待機が必要。
