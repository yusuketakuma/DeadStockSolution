# Dev Assets

このディレクトリには、アプリ本体とは別に運用する開発補助資産をまとめる。

- `e2e/`: root Playwright config から参照するブラウザ監査・E2E spec
- `remotion/`: 動画生成用コンポジション
- `test-data/`: 手動検証や fixture 再生成用のサンプル Excel

repo-local Codex ルールは tool discovery の都合で root `.codex/` に置く。
