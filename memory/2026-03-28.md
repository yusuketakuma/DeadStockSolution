# 2026-03-28

- `Plans.md` の未チェック `T*` は解消済み。
- `T1126` は proposal-flow Playwright を CI に接続するまで補完した。
  - `.github/workflows/ci.yml` に `proposal-flow-e2e` job を追加。
  - `scripts/run-proposal-flow-e2e.sh` を追加し、migrate -> test pharmacy seed -> server/client 起動 -> Playwright happy path を一括化。
- reviewer 指摘で `T1133` に未達が見つかったため追加修正。
  - direct message 受信を preference-aware push に接続。
  - admin pharmacy message を push + dashboard unread cache invalidation に接続。
  - admin user request reply を `createNotification` 経由に変更。
  - OpenClaw callback/report の完了通知を `createNotification` 経由へ寄せ、failed report も通知対象に追加。
- ローカル実働 E2E は未実施。
  - `server/.env` と `.env.preview.migrate` が preview Neon DB を向いており、そのまま `npm run dev:server` は危険。
  - このマシンの `localhost:5432` に disposable Postgres はいなかった。
